package com.grimclient.modules.client;
import com.grimclient.modules.Module;
public class AutoConfig extends Module {
    public AutoConfig() { super("AutoConfig", "Auto loads config on server join", Category.CLIENT); }
}
